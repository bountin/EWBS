% test 7 for battery

% state: half_full
% input power: 10
% demanded power: 10

% exp. result: output of 10

battery(b).
const(cb, 10).

c(b, is, half_full).
c(b, id, 10).
p(b, ip, 10).

expect_c(b, id, 10).
expect_c(b, is, half_full).
expect_p(b, ip, 10).

expect_p(b, op, 10).
