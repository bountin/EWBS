% Testfall 4 fuer Generator

% Strombedarf von Generator: 0
% Generator: not running

% Ergebnis: Generator soll 0 liefern

generator(g).
const(cg, 20).

c(g, is, not_running).
c(g, id, 0).

expect_c(g, id, 0).
expect_c(g, is, not_running).
expect_p(g, op, 0).
