% test 11 for battery

% state: empty
% input power: 10
% demanded power: 4

% exp. result: output of 4

battery(b).
const(cb, 10).

c(b, is, empty).
c(b, id, 4).
p(b, ip, 10).

expect_c(b, id, 4).
expect_c(b, is, empty).
expect_p(b, ip, 10).

expect_p(b, op, 4).
